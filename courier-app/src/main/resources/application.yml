server:
  port: 8080

spring:
  application:
    name: courier-app
  kafka:
    bootstrap-servers:
      - kafka:9092
    consumer:
      auto-offset-reset: earliest

logging:
  file:
    name: /var/log/courier-app/app.log